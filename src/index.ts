let menorValor : number =3
let mayorValor : number =7
let edadesAlumnos : [] = new Array(10)
let sumaEdades : number;
function aleatorio(mayorValor: number, menorValor) {
  return Mat.floor(mat.random () * (mayorValor - menorValor +1)) + menorValor;
 
  for (let i 0; = 0; < edades.Alumnos.length; i++) {
    edadesAlumnos = arr0;];
    
  }
  
}
